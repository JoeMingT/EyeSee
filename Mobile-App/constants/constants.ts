import { documentDirectory } from 'expo-file-system';

const IMAGE_DIR_PATH = documentDirectory + '/Images';
const DATABASE_PATH = documentDirectory + '/data.json';

export { IMAGE_DIR_PATH, DATABASE_PATH };
